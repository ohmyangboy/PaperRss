# 04 — 交付 B/N 双按导航并校正空格确认

**What to build:** 用户可以通过 B/N 在当前中间栏列表中安全查看上一篇或下一篇；第一次按键只提示，必须在 Toast 消失前再次按下同一个键才切换，避免误触和长按连续跳过文章。

**Blocked by:** 01 — 建立无冲突的阅读快捷键入口并交付 C 对照翻译。

**Status:** resolved

- [x] B/N 严格按当前中间栏可见筛选与排序定位上一篇/下一篇，不跨筛选、不循环。
- [x] 未读列表在当前文章打开后仍能稳定定位相邻项；切换文章沿用现有自动标记已读行为。
- [x] 第一次按 B/N 显示 2.5 秒底部 Toast；只有时限内再次按下同一个键才切换。
- [x] 超时、改变方向或触发其他阅读快捷键会清空确认，系统 key repeat 不构成第二次按键。
- [x] 列表边界直接提示，不进入待确认状态；空格、B、N 不能互相充当第二次确认。
- [x] 现有空格切换改为 Toast 消失即清空确认，保持其余滚动和到底切文行为不变。
- [x] 阅读胶囊帮助提示包含 B/N，状态机回归测试、完整测试、构建和应用启动验证通过。

**Answer:** B/N 与空格共用 2.5 秒、按键隔离的确认状态机；当前可见列表保留已打开未读项。Debug 应用实测首按显示 Toast、二按切换，B 可返回原文章，测试产生的已读状态已恢复。
