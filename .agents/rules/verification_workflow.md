# [Always On] 应用代码修改自验证规则

## 强制约束
任何时候只要对项目代码（如 Swift, HTML, JS, CSS, 配置文件等）进行了修改，**必须在修改完成后无条件立即执行 `scripts/dev.sh`**（或项目对应重启脚本）进行重新编译与应用重启自验证。

## 执行要求
1. 完成代码文件编辑后，不得直接结束任务。
2. 必须立即通过 `run_command` 调用 `./scripts/dev.sh`。
3. 检查并确认编译成功、应用正常拉起。
