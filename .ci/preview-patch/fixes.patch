--- a/Tests/AppFeatureRegressionTests.swift
+++ b/Tests/AppFeatureRegressionTests.swift
@@ -18,7 +18,8 @@
     var store: AppStore!
 
     override func setUp() async throws {
-        try await super.setUp()
+        // XCTestCase's default async setup is a no-op. Do not send this
+        // MainActor-isolated fixture to its nonisolated async superclass.
         tempDir = FileManager.default.temporaryDirectory
             .appendingPathComponent("PaperRssFeatureRegression-\(UUID().uuidString)")
         try FileManager.default.createDirectory(at: tempDir, withIntermediateDirectories: true)
@@ -40,7 +41,6 @@
         if let tempDir {
             try? FileManager.default.removeItem(at: tempDir)
         }
-        try await super.tearDown()
     }
 
     // MARK: - 非阻断式瞬时提示 (toast) 冷却去重回归
--- a/Tests/ArticleRetentionPolicyTests.swift
+++ b/Tests/ArticleRetentionPolicyTests.swift
@@ -7,7 +7,8 @@
     var store: AppStore!
 
     override func setUp() async throws {
-        try await super.setUp()
+        // XCTestCase's default async setup is a no-op. Do not send this
+        // MainActor-isolated fixture to its nonisolated async superclass.
         store = AppStore(testDatabase: AppDatabase.empty, feedFetcher: { _ in
             FeedFetchResult.notModified(etag: nil, lastModified: nil)
         })
@@ -15,7 +16,6 @@
 
     override func tearDown() async throws {
         store = nil
-        try await super.tearDown()
     }
 
     // MARK: - Helpers
--- a/Tests/CacheManagementTests.swift
+++ b/Tests/CacheManagementTests.swift
@@ -12,7 +12,8 @@
     var store: AppStore!
 
     override func setUp() async throws {
-        try await super.setUp()
+        // XCTestCase's default async setup is a no-op. Do not send this
+        // MainActor-isolated fixture to its nonisolated async superclass.
         store = AppStore(testDatabase: AppDatabase.empty, feedFetcher: { _ in
             FeedFetchResult.notModified(etag: nil, lastModified: nil)
         })
@@ -20,7 +21,6 @@
 
     override func tearDown() async throws {
         store = nil
-        try await super.tearDown()
     }
 
     // MARK: - Helpers
--- a/Tests/sparkle-local-real-upgrade.test.mjs
+++ b/Tests/sparkle-local-real-upgrade.test.mjs
@@ -18,9 +18,9 @@
 async function run(command, args, options = {}) {
   return execFile(command, args, {
     cwd: root,
-    env: { ...process.env, ...options.env },
     maxBuffer: 1024 * 1024,
     ...options,
+    env: { ...process.env, ...options.env },
   });
 }
 
--- a/PaperRss/Sources/App/TimelineImageViews.swift
+++ b/PaperRss/Sources/App/TimelineImageViews.swift
@@ -225,7 +225,7 @@
                         }
                     }
                     Divider()
-                    Toggle(I18N.localized("显示文章配图"), isOn: Binding(get: { showsImages }, set: onToggleImages))
+                    Toggle(I18N.localized("显示文章配图"), isOn: Binding(get: { showsImages }, set: { value in onToggleImages(value) }))
                     Text(I18N.localized("图片按需从原网站加载，不会自动抓取原文网页。"))
                         .font(.caption).foregroundStyle(.secondary)
                         .fixedSize(horizontal: false, vertical: true)
